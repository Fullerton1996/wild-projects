[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Note: GEMINI_API_KEY must be set in Netlify dashboard under Site Settings > Environment Variables
  # This is required for the app to function properly

